name: Timer drag reverse fix
on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/timer-drag-reverse-fix.yml'
jobs:
  patch:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Apply reverse timer drag fix
        run: python3 .github/scripts/timer_drag_reverse_fix.py
      - name: Validate timer script
        run: node --check budget/exercise-timer-focus.js
      - name: Commit reverse timer drag fix
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add budget/exercise-timer-focus.js budget/exercise.html budget/auth-config.js budget/auth-gate.js budget/exercise-pulse-flow-canvas-glow-v131.js
          git diff --cached --quiet || git commit -m 'Fix reverse timer drag geometry and backdrop'
          git push
